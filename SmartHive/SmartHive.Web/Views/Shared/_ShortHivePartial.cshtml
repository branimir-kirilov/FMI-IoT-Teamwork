@model SmartHive.Web.Models.ShortHiveViewModel

<div class="container well">
    <h3 class="media-heading">
       @* @Html.ActionLink(, "Details", new { controller = "Post", id = Model.Id })*@
    </h3>
   @* <img src="@Model.ImageUrl" alt="Alternate Text" class="post-short-pic" />*@
    <ul class="list-inline list-unstyled">
     @*   <li><span><i class="glyphicon glyphicon-calendar"></i> @string.Format("{0:D} at {0:HH:mm}", Model.DatePublished)</span></li>*@
    </ul>
</div>
